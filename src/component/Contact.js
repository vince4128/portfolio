import React from 'react';

const Contact = (props) => {
    return(
        <section id="contact" className="section--contact">
            <h1>Contact</h1>
        </section>
    )
}

export default Contact;