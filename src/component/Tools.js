import React, { component } from 'react';

const Tools = (props) => {
    return(
        <section id="tools" className="section--tools">
            <h1>Tools</h1>
        </section>
    )
}

export default Tools;